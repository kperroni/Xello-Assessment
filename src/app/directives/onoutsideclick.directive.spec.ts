import { OnoutsideclickDirective } from './onoutsideclick.directive';

describe('OnoutsideclickDirective', () => {
  it('should create an instance', () => {
    const directive = new OnoutsideclickDirective();
    expect(directive).toBeTruthy();
  });
});
